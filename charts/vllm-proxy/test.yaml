# Example test configuration for vllm-proxy
proxyEndpoints:
  - endpoint: http://vllm-model-1:8080
    ingress:
      host: model1.hnatekmar.dev

proxy:
  replicas: 1
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "256Mi"
