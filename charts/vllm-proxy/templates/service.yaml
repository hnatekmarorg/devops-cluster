apiVersion: v1
kind: Service
metadata:
  name: {{ include "vllm-proxy.fullname" . }}
  labels:
    {{- include "vllm-proxy.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vllm-proxy.selectorLabels" . | nindent 4 }}
