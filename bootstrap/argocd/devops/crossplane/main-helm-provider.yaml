apiVersion: helm.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: main
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  credentials:
    secretRef:
      key: value
      name: main-kubeconfig
      namespace: default
    source: Secret