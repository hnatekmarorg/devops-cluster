# Charts Overview
- **`cluster-base`**: Deploys foundational infrastructure (Crossplane, Nginx Ingress, NFS storage) required by other charts.
- **`llama-cpp`**: Deploys llm models with GPU/CPU support, including automatic scaling and ingress integration.